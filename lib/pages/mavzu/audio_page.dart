import 'package:flutter/material.dart';

class AudioPage extends StatelessWidget {
  final String audioUrl;
  const AudioPage({super.key, required this.audioUrl});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
