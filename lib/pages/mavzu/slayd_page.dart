import 'package:flutter/material.dart';

class SlaydPage extends StatelessWidget {
  final String slaydUrl;
  const SlaydPage({super.key, required this.slaydUrl});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
